#include<bits/stdc++.h>

// first posiition waale ko sorted smaajhke second pe move krenge, the accordingly usse move krenge
// exmaple is in the picture!

void myFun(){

}


int main(){


    int n;
    std::cin>>n;
    // declare n, input lo n ki value ka uske baad declare the array
    int myArr[n];
    // Taking inpur of the array
    for(m=0;m<n;m++){
        std::cin>>myArr[m];
    }
    return 0;
}